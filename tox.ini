# tox configuration
[tox]
envlist = py3
skipsdist = True

[gh-actions]
python =
    3.9: py3

[testenv]
deps =
    pytest
    pytest-xdist
    pytest-split
    cocotb
    cocotb-test
    cocotbext-axi
    jinja2

commands =
    pytest -n auto {posargs}

# pytest configuration
[pytest]
testpaths =
    tb
addopts =
    --ignore-glob=tb/test_*.py
    --import-mode importlib
